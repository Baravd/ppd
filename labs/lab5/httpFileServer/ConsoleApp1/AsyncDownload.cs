using System;
using System.IO;
using System.Net;
using System.Runtime.CompilerServices;
using System.Threading.Tasks;

namespace ConsoleApp1
{
    public static class AsyncDownload
    {
       /* public static async Task<string> ProcessUrlAsync(string url)
        {
            /*using (var client = new WebClient())
            {
                using (var data = client.OpenRead())
                {
                    Console.WriteLine(data);
                    using (var reader = new StreamReader(data))
                    {
                        var s = reader.ReadToEnd();
                        Console.WriteLine("Here:");
                        var fileLength = client.ResponseHeaders["Content-Length"];

                        Console.WriteLine(fileLength);
                        data.Close();
                        reader.Close();
                    }
                }
                

                     // var clientResponseHeader = client.ResponseHeaders["Content-Length"];
                return new string("aa");

            }#1#
        }*/
        
    }
}